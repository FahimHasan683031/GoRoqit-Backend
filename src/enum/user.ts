export enum USER_ROLES {
    ADMIN = 'admin',
    USER = 'user',
    GUEST = 'guest',
    CUSTOMER = 'applicant',
}


export enum USER_STATUS {
    ACTIVE = 'active',
    RESTRICTED = 'restricted',
    DELETED = 'deleted',
}