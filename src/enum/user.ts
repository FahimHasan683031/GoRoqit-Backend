export enum USER_ROLES {
  ADMIN = 'admin',
  APPLICANT = 'applicant',
  RECRUITER = 'recruiter',
}

export enum USER_STATUS {
  ACTIVE = 'active',
  RESTRICTED = 'restricted',
  DELETED = 'deleted',
}
